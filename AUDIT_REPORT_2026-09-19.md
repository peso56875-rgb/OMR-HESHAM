# تقرير الفحص الأمني الشامل — مؤسسة عمر هشام الخيرية
**التاريخ:** 19 سبتمبر 2026
**النطاق:** مشروع الموقع (Hono API + Firebase Firestore + Cloudinary) — مراجعة IDOR وأمن شامل + إصلاحات

---

## خلاصة الجلسة

تم اكتشاف **9 ثغرات جديدة** (V1–V9) وتم إصلاحها جميعاً والتحقق منها، بالإضافة إلى تأكيد سلامة إصلاحات الجلسة السابقة (C1–C3).

> **تحديث (جلسة اليوم):** V5 (تسريب بيانات المتطوعين عبر الشهادة والبطاقة) مكتشفة ومصلَّحة ومتحقَّق منها فعلياً، و **V6 (خطيرة — قواعد Firestore المفتوحة على الإنشاء الجماعي)** مكتشفة ومصلَّحة، مع V7/V8 (Quran) و V9 (تنظيف كود ميت) — التفاصيل أدناه.
>
> **✅ تحديث (النشر والإغلاق):** تم **نشر جميع الإصلاحات إلى الإنتاج** — قواعد Firestore المقفلة (ruleset جديد مؤكَّد حياً) + دفع الكود إلى Vercel (نشر تلقائي مُتحقَّق بسلوك حي)، ثم دفعة تحصين أخيرة أزالت المعرّف الداخلي من استجابة التحقق. القسم الأخير من التقرير.

---

## الثغرات الجديدة التي تم إصلاحها

### 🔴 V1 (خطيرة) — تضخيم ساعات المتطوعين + غياب فحص الحالة في المهام الميدانية
**الملف:** `src/api/volunteers.ts`

| المشكلة قبل الإصلاح | الوضع بعد الإصلاح |
|---|---|
| `/missions/checkin` يقبل ساعات يرسلها العميل بدون سقف (`Number(body.hours)`) | الساعات تُؤخذ حصراً من قاعدة البيانات (`volunteer_missions`) أو المهام الافتراضية، وبسقف أقصى **12 ساعة**، مع تجاهل أي ساعات يرسلها العميل |
| تكرار checkin بلا حدود لنفس المهمة | منع تكرار: حضور واحد فقط لنفس المهمة في نفس اليوم (استعلام `volunteer_id` + فلترة بالذاكرة — بدون الحاجة لمؤشر مركّب) |
| أي طالب (قيد المراجعة/مرفوض) يستطيع الانضمام للمهام وتسجيل حضور | اشتراط `status === 'approved'` في `/missions/join` و `/missions/checkin` |
| الانضمام لأي mission_id وهمي (افتراضي 3 ساعات) | التحقق من وجود المهمة وتفعيلها (من DB أو المهام الافتراضية) قبل الانضمام |

### 🔴 V2 (خطيرة) — تخزين XSS عبر اسم قارئ الختمة (reader_name)
**الملفات:** `src/api/quran.ts` + `src/components/Quran.tsx`

- **قبل:** نقطة `/khatma/claim` عامة بدون مصادقة، تخزّن أي `reader_name`، ويتم حقنه في الصفحة عبر `innerHTML` بدون تهريب → أي زائر مجهول يستطيع تنفيذ JavaScript في متصفح **كل الزوار والمشرفين**.
- **بعد — دفاع متعدد الطبقات:**
  1. **الخادم:** `sanitizeKhatmaName()` تحذف `[<>&"'`]` والأحرف التحكمية، وتوحّد المسافات، وتقصّ الطول عند 60 حرفاً.
  2. **العميل:** دالة `escHtml()` تهرب `reader_name` **و `title`** قبل إدخالهما في `innerHTML` — وهذا يحيد أيضاً أي حمولة خبيثة قد تكون مخزّنة بالفعل في قاعدة البيانات (حتى تدور الختمة).

### 🟠 V3 (متوسطة) — التلاعب بالختمة بدون مصادقة أو ملكية
**الملف:** `src/api/quran.ts`

| المسار | قبل | بعد |
|---|---|---|
| `/khatma/claim` | عام، أي شخص يحجز أي جزء | `authMiddleware` + `rateLimiter(20/دقيقة)` + ربط الجزء بـ `user_id` + رفض 409 إذا كان محجوزاً من قارئ آخر |
| `/khatma/release` | أي شخص يلغي حجز أي جزء | تسجيل دخول + صاحب الحجز أو مشرف فقط (الحجوزات القديمة بدون `user_id` قابلة للإلغاء من أي مستخدم مسجل — مقايضة موثقة) |
| `/khatma/complete` | أي شخص يكمل أي جزء ويُدير الختمة | تسجيل دخول + صاحب الحجز أو مشرف + يتطلب `status === 'reading'` |

> نفس الحواجز مطبَّقة في فرع النسخ الاحتياطي بالذاكرة (عند انقطاع قاعدة البيانات).

### 🟠 V4 (متوسطة) — أكواد المتطوعين التسلسلية + تسريب بيانات شخصية في /verify
**الملف:** `src/api/volunteers.ts`

- **قبل:** الأكواد تسلسلية (`VOL-1`, `VOL-2`...) تسمح بتعداد كل المتطوعين، ونقطة `/verify/:code` العامة ترجع `full_name` و `avatar_url` و `preferred_role` (بيانات شخصية).
- **بعد:**
  1. الأكواد الجديدة: `VOL-<الرقم>-<4 أحرف عشوائية>` من أبجدية بدون أحرف ملتبسة (`ABCDEFGHJKLMNPQRSTUVWXYZ23456789`) عبر CSPRNG — لا يمكن تخمينها (نطاق ~1.1 مليون احتمال للرمز الواحد).
  2. استجابة `/verify` أصبحت بلا بيانات شخصية: فقط `id, volunteer_code, team, rank, hours_count, approved_at, expires_at, status, is_active` + أعلام الصلاحية.

### 🔴 V5 (خطيرة) — تسريب بيانات المتطوعين عبر صفحات الشهادة والبطاقة (الأكواد التسلسلية)
**الملف:** `src/index.tsx` — المسارات `/certificate/:id` و`/volunteers/card/:id`

- **قبل:** المتسلل يستطيع تعداد المتطوعين عبر الأكواد التسلسلية القديمة (`VOL-1`, `VOL-2`, ...) وفتح `/volunteers/card/VOL-1` أو `/certificate/VOL-1` بدون تسجيل دخول ليكشف **الاسم الكامل** (مثال مؤكد حياً: «هشام محمد صبري») + الصورة + رمز المتطوع. النطاق ذو الصلة جزء من جلسة سابقة تم التحقق منه حياً على الإنتاج.
- **بعد (دفاع من ثلاث طبقات):**
  1. **صفحات الرفض** `لا يمكنك عرض هذه الشهادة` / `لا يمكنك عرض هذه البطاقة` تُعرض لأي زائر **ليس** مالك المتطوع أو مشرفاً — بلا أي بيانات شخصية في الصفحة ولا في `error`/`status` المنطقي.
  2. **حارس الملكية** `isOwner = user && volunteer.profile_id === user.id` أو تطابق البريد (بصيغة تعامُل مع البريد المسجّل في السجل مقابل بريد الجلسة، مع معالجة حالة `profile_id` الغائب في السجلات القديمة) + `isAdmin` للوصول الكامل.
  3. **صفحة الشهادة المقفلة** (قبل اكتمال الساعات) لم تعد تقرأ `volunteer.full_name` إطلاقاً في الـ JSX — سابقة كانت موجودة بالاسم في شجرة العرض المقفلة.
- **التحقق الفعلي (خادم محلي، قاعدة بيانات حقيقية):**
  - `GET /volunteers/card/VOL-1` بدون جلسة → `200` مع صفحة رفض (علامات: `لا يمكنك عرض هذه البطاقة`) — **الاسم «هشام محمد صبري» غير موجود نهائياً** في HTML (المطابقات الوحيدة لـ «هشام» في الصفحات العامة تأتي من اسم الموقع «مؤسسة د. عمر هشام» في وسوم meta).
  - `GET /certificate/VOL-1` بدون جلسة → `200` مع صفحة رفض (`لا يمكنك عرض هذه الشهادة`) — لا اسم ولا صورة.
  - `GET /api/volunteers/verify/VOL-1` → `200` JSON بلا PII: `{found, revoked, expired, status, volunteer:{id, volunteer_code, team, rank, hours_count, approved_at, expires_at, is_active}}`.

### 🔴 V6 (خطيرة) — قواعد Firestore مفتوحة على الإنشاء الجماعي (دون تبرعات/متطوعين/نشرة/تواصل/وظائف)

- **الخلل:** كانت القواعد تسمح لأي زائر مجهول بالكتابة مباشرة من المتصفح عبر Firestore REST API:
  - `donations` و `volunteers`: `allow create: if true`
  - `newsletter_subscribers`: `allow create, update: if true`
  - `contacts` و `job_applications`: إنشاء مفتوح
- **الاستغلال الممكن:**
  - تزوير تبرعات بحالة `status: 'completed'` → **تضخيم إجمالي التبرعات** المعروض علنياً في الصفحة الرئيسية (`where('status','==','completed')` في index.tsx).
  - صنع وثائق متطوعين بحالة `status: 'approved'` مباشرة، **متجاوزاً تحقق الخادم** في `POST /api/volunteers` (الذي يفرض `status:'pending'` + `is_active:false`) ومتجاوزاً حدود المعدل.
  - استبدال/إعادة كتابة مستندات مشتركي النشرة البريدية بقيم مُدخَلة (تلويث بيانات + هجمات محتملة على عمليات الإرسال الجماعي).
- **الإصلاح المطبق (`firestore.rules`):**
  - `donations`/`volunteers`: قراءة للمالك (`profile_id`) أو المشرف فقط؛ **الكتابة ممنوعة من العميل نهائياً**.
  - `contacts`/`job_applications`/`newsletter_subscribers`: **مشرفون فقط** (الإنشاء يتم حصرياً عبر الخادم/Admin SDK).
  - الإبقاء على قاعدة deny-all الشاملة كنهاية قاطعة.
- **الأمان ضد الإغلاق:** تم تدقيق قاعدة الكود كاملة — **لا توجد أي كتابة Firestore من المتصفح** في `src/` أو `public/`؛ الاستخدام الوحيد لـ web SDK هو `Auth.tsx` (تسجيل دخول Google — auth فقط) وتسجيل FCM عبر `/firebase-messaging-sw.js`. كل إدخالات Firestore تتم عبر Admin SDK الخادمي الذي **يتجاوز القواعد**، لذا قفل قواعد العميل لا يكسر أي وظيفة.
- **التحقق:** قواعد المصادقة على المنطق مراجعة سطراً بسطر + لا استخدامات عميل مهددة + البناء ينجح. (نشر القواعد إلى Firebase يُفعّل الحماية — لم يتم من هذه البيئة.)

### 🟢 V7 (منخفضة) — حقن/شكل ترويسة Content-Disposition في `/api/quran/audio`

- **الخلل:** المعامل `reciter` كان يُدرَج خاماً في اسم الملف داخل `Content-Disposition` — قيمة ضارة (اقتباسات/أسطر جديدة) قد تكسر صيغة الترويسة أو تحقن فيها.
- **الإصلاح:** تعقيم `reciter` بأحرف `[a-zA-Z0-9_-]` وبحد أقصى 32 حرفاً + **سقوط آمن** لأي قارئ غير معروف إلى `minshawi` قبل توليد الرأس (الاسم يُستخدم أصلاً كمفتاح خريطة ثابتة فقط — لا SSRF).
- **التحقق الفعلي (خادم محلي):** طلب بمعرّف قارئ ضار `ab"\r\nX-Evil: yes` → رد `200` برأس نظيف `attachment; filename="Surah_001_minshawi.mp3"` و**لا يوجد** رأس `X-Evil` محقون.

### 🟢 V8 (منخفضة) — غياب تحديد المعدل على `/api/quran/khatma/release` و `/complete`

- **الخلل:** `khatma/claim` كان محدوداً (20/دقيقة) لكن `release` و `complete` بلا حدود — سهل الاستغلال للتشويش/إلغاء حجوزات الآخرين بصورة متكررة بعد تخمين معرّفات الأجزاء.
- **الإصلاح:** `rateLimiter(10, 60000)` لكلٍ منهما (بعد `authMiddleware` — لا يؤثر على الرفض غير المصادق).
- **التحقق:** الطلبات غير المصادقة → `401` قبل الوصول إلى المحدد (ترتيب الوسطاء سليم).

### 🧹 V9 (تنظيف كود ميت) — `verificationUrl` و `isPublicVerification`

- **الإزالة:** المتغير `verificationUrl` المُولَّد في `index.tsx` (~سطر 616) + معامل `verificationUrl` والخاصية غير المستخدمة `isPublicVerification` في `Certificate.tsx` — كانا بلا استخدام نهائياً (لا مسار `/verify-certificate` ولا عرض فعلي).
- **التحقق:** `rg verificationUrl` → لا نتائج؛ `rg isPublicVerification` → لا نتائج؛ `tsc --noEmit` سليم.

---

## نقاط تم فحصها وتأكيد سلامتها (بدون إجراء)

| الوحدة | النتيجة |
|---|---|
| `profile.ts` | الهوية من الجلسة فقط، لا تعديل دور |
| `users.ts` | تغييرات الأدوار خلف `adminMiddleware` |
| `receipts.ts` + `/receipt/verify` | توقيع HMAC-SHA256 (80-bit) بمقارنة ثابتة الزمن + تحديد معدل الطلبات |
| `notifications.ts` | كل العمليات مربوطة بـ uid الجلسة — لا IDOR |
| `search.ts` / `audit.ts` / `export.ts` / `medical.ts` | محمية أو للقراءة فقط |
| `firestore.rules` | **مقفلة الآن بالكامل** — لا كتابة عميل إطلاقاً (إنشاء عبر الخادم فقط)، قراءة للمالك/المشرف، وسقف deny-all |
| روابط القرآن الصوتي/الراديو | قائمة روابط ثابتة موثوقة — لا SSRF |

---

## التحقق النهائي (فِعلي)

1. **فحص الأنواع:** `tsc --noEmit` → **0 أخطاء**
2. **البناء الإنتاجي:** `npm run build` (vite + postbuild-vercel) → **نجاح**
3. **فحص حي على خادم محلي:**
   - `GET /volunteers/card/VOL-1` بدون جلسة → **200 صفحة رفض بدون اسم** (كانت تسرّب الاسم الكامل)
   - `GET /certificate/VOL-1` بدون جلسة → **200 صفحة رفض بدون اسم** (كانت تسرّب الاسم الكامل)
   - `GET /api/volunteers/verify/VOL-1` → **200 JSON بلا PII**
   - `POST /api/quran/khatma/{claim,release,complete}` بدون جلسة → **401** (كانت مفتوحة)
   - `POST /api/volunteers/missions/checkin` بدون جلسة → **401**
   - `GET /api/quran/khatma/current` → **200** (لا تزال عامة)
   - `GET /api/volunteers/verify/...` → **200** بدون تسريب PII
4. **التغييرات (جلسة V6):** 4 ملفات — `firestore.rules` (قفل كتابة العميل)، `src/api/quran.ts` (تعقيم reciter + rate limiters)، `src/components/Certificate.tsx` + `src/index.tsx` (حذف الكود الميت).
5. **فحص حي إضافي (V6):**
   - `GET /api/quran/khatma/current` → **200** (Firestore Admin SDK يعمل)
   - `POST /api/quran/khatma/{claim,release,complete}` بدون جلسة → **401** (ترتيب الوسطاء سليم)
   - `GET /api/quran/audio/minshawi/1?download=1` → رأس `Content-Disposition: attachment; filename="Surah_001_minshawi.mp3"` نظيف
   - `GET /api/quran/audio/` + قارئ ضار (`ab"\r\nX-Evil: yes`) → **200** ورأس نظيف بلا رأس محقون `X-Evil` (تعقيم V7 يعمل)
   - `GET /` + `/api/campaigns,events,news,programs,gallery,jobs,stories` → **200** (لا انحدار)

---

## ملاحظات وتوصيات متبقية

- **✅ الإنتاج محمي الآن:** تم نشر قواعد Firestore المقفلة والكود المصلَّح (V1–V9) — تفاصيل الإثبات في قسم النشر أدناه.
- **✅ منخفضة (أُصلحت في الدفعة الأخيرة) — معرّف المستند الداخلي في `/api/volunteers/verify`:** أُزيل `id` من الاستجابة (الالتزام `d5b1135`) وتحقَّقنا حياً من اختفائه من الإنتاج.
- **🟡 ملاحظة على الريبو:** الريبو **عام (public)** وقد انتقل مؤخراً من `kato43877-gif/OMR-HESHAM` إلى `peso56875-rgb/OMR-HESHAM` (رُحّل الـ remote محلياً). فُحص كل تاريخ git — **لا توجد أسرار مُلتزَمة** (`.env` مستثنى، ومفاتيح Firestore/Cloudinary تظهر فقط كأسماء متغيرات). يوصى بمراجعة إعدادات الرؤية/الصلاحيات.
- **أولوية مستقبلية مقترحة:** فصل صلاحية "صاحب الحجز" عن `role === 'admin'` في كتمة القرآن إن أُضيفت لاحقاً رتب وسيطة (تمت إضافة الـ rate limiters في V8).
- **بيانات قديمة:** أي حمولة XSS مخزّنة قديماً في `quran_khatmas/active` ستظهر كنص آمن (مهرَّب) فور نشر إصلاح الواجهة — لا حاجة لمسح يدوي.
- **مقايضات موثقة:** حجز الختمة أصبح يتطلب تسجيل دخول (متسق مع انضمام المهام)؛ الحجوزات القديمة بدون مالك قابلة للإلغاء من أي مستخدم مسجل.
---

## ✅ النشر إلى الإنتاج — تم (19 سبتمبر 2026)

### 1) قواعد Firestore — نُشرت وفُحصت حياً

| | قبل | بعد |
|---|---|---|
| Ruleset | `956156d8-7284-4b56-ae7f-3b1b642a545c` (هش) | `81550af8-c09c-4142-b145-1c824880ad92` (مقفل) |
| `donations` / `volunteers` | `allow create: if true` ⚠️ | `write: if false` — قراءة owner/admin فقط |
| `contacts` / `job_applications` | `allow create: if true` ⚠️ | `isAdmin()` فقط |
| `newsletter_subscribers` | create **و** update مفتوحان ⚠️ | `isAdmin()` فقط |
| Catch-all | — | `deny` صريح للسجلات غير المتوافقة |

- **الأمر:** `node scripts/deploy-rules.mjs` (خدمة-حساب من `.env`، API إدارة Firebase).
- **التحقق الحي بعد النشر:** `node scripts/verify/rules.mjs` (أداة جديدة مُلتزَمة في `scripts/verify/rules.mjs`) جلبت الـ ruleset المنشور فعلياً من خدمة Rules API وأكدت: `donations`/`volunteers` → `write: if false`، `contacts`/`job_applications`/`newsletter_subscribers` → `isAdmin`، ووجود سقف deny-all.
- **أهمية هذا الإصلاح:** هذه هي الثغرة الحرجة V6 — أي زائر مجهول كان يستطيع إنشاء مستندات في كل هذه المجموعات (تسمّم البيانات/الانتحال/إغراق الاشتراكات). الآن **لا كتابة عميل إطلاقاً** — كل الكتابة تمر بالخادم (Admin SDK) عبر Hono API.

### 2) الكود — دُفع إلى Vercel ونُشر تلقائياً

- **الالتزامات المدفوعة إلى `origin/main`:** `7c715b5` (إصلاحات V1–V9 الكاملة) + `5ce3137` (أداة التحقق `scripts/verify/rules.mjs`) + `d5b1135` (تحصين إضافي: rate limiter للبحث والتحقق، إزالة المعرّف الداخلي من `id` استجابة `/verify`، تقوية مسارات البوابة/الشهادة/الملف الشخصي — نُشر وتم التحقق منه حياً).
- **الـ remote:** حُدِّث إلى `https://github.com/peso56875-rgb/OMR-HESHAM.git` بعد نقل الريبو (تحذير redirect من `kato43877-gif/OMR-HESHAM`).
- **النشر:** تكامل Vercel GitHub → نشر تلقائي؛ **المؤشر (canary)**: `GET /api/quran/audio/ab%22%0D%0AX-Evil:%20yes/1?download=1` غيّر سلوكه من **302** (الكود القديم) إلى **200** مع `Content-Disposition: attachment; filename="Surah_001_minshawi.mp3"` نظيف وبدون رأس `X-Evil` (~18:41).

### 3) جدول التحقق بعد النشر (إنتاج حي — كلها نجحت)

| المسار/الفحص | النتيجة المتوقعة | النتيجة الفعلية |
|---|---|---|
| `GET /api/quran/audio` + قارئ ضار `ab"\r\nX-Evil: yes` (`?download=1`) | 200، رأس نظيف، بلا `X-Evil` | ✅ 200 نظيف (تعقيم V7 حي) |
| `GET /api/quran/audio/minshawi/1?download=1` | 200 + `Surah_001_minshawi.mp3` | ✅ 200 سليم |
| `POST /api/quran/khatma/{claim,release,complete}` بدون جلسة | 401 | ✅ 401 (الحدود V8 حية) |
| `GET /api/volunteers/verify/VOL-1` | 200 بدون PII وبدون `id` داخلي | ✅ V5 حي + المعرّف الداخلي اختفى من الاستجابة (`d5b1135`) |
| المسارات العامة `/` و `/api/campaigns,events,news,programs,gallery,jobs,stories` | 200 | ✅ لا انحدار |
| `GET /api/quran/khatma/current` | 200 | ✅ Firestore Admin SDK يعمل |

### 4) فحص الانحدار — لا شيء يُكسر بقفل القواعد

- `public/static/*.js` (الواجهة القديمة `app.js?v=2.8`): **صفر** استخدام SDK/HTTP مباشر لقواعد Firestore — لا كتابة من المتصفح.
- الكود الحالي (`src/`): جميع الكتابات عبر Hono API → Admin SDK (يتجاوز القواعد بصلاحيات الخادم).
- سكربت الجدولة `scripts/cron/notification-digest.mjs` يستخدم Admin SDK → لا يتأثر.
- **الخلاصة:** لا يوجد أي مسار كتابة من العميل؛ القفل لا يكسر ميزة واحدة قائمة.

### 5) ما تبقى (أولوية منخفضة — موثّق فقط)

- مراجعة إعدادات رؤية الريبو العام `peso56875-rgb/OMR-HESHAM` — فُحص كل تاريخ git ولا توجد أسرار مُلتزَمة.
- ~~إزالة المعرّف الداخلي من `/verify`~~ — **تمت**: المعرّف الداخلي أُزيل في `d5b1135` والاستجابة الحية نظيفة منه.
- **الحالة: مغلقة ✅** — كل الأهداف الرئيسية (V1–V9) في الإنتاج وتم التحقق منها حياً.
