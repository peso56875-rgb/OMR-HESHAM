# خطة التطوير الشاملة — مؤسسة الدكتور عمر هشام الخيرية

> وثيقة فحص كامل للموقع + خطة تطوير مرتّبة بالأولوية للوصول لموقع مؤسسة خيرية احترافي متكامل.
>
> **تاريخ الفحص:** 2026-08-05 · **الإصدار الحالي:** 2.0.0
> **نطاق الفحص:** 20 ملف API · 12 مكوّن واجهة · 24 مسار صفحة · 2440 سطر CSS · قواعد Firestore

---

## 1. ملخّص تنفيذي

الموقع في حالة **جيدة جدًا من ناحية الواجهة والتصميم** — تجربة RTL عربية كاملة، وضعين فاتح وداكن، تصميم متجاوب، ولوحة تحكم غنية بـ 17 قسمًا. البنية التقنية سليمة (Hono + Firebase + JSX على الخادم).

لكن الفحص كشف **فجوات جوهرية في 3 محاور** لازم تُسدّ قبل ما نوصف الموقع بأنه "مؤسسة خيرية احترافي بالكامل":

| المحور | التقييم | الخلاصة |
|---|---|---|
| التصميم وتجربة المستخدم | 8.5/10 | ممتاز، يحتاج صقل |
| الأمان والحماية | **4/10** | ⚠️ فيه ثغرات حقيقية لازم تُصلح فورًا |
| سلامة البيانات المالية | **5/10** | ⚠️ فيه باج بيضيّع ربط التبرعات بالحملات |
| الثقة والشفافية (متطلب خيري) | 6/10 | ناقص إيصالات وتقارير |
| الجاهزية للتشغيل (Ops) | **3/10** | مفيش تنبيهات، مفيش اختبارات، مفيش نسخ احتياطي |
| SEO والوصول | 6.5/10 | أساسيات موجودة، ناقص تفاصيل |

**أهم 5 حاجات لازم تتعمل حالًا:** ثغرة تزوير الجلسة · باج ربط التبرع بالحملة · قواعد Firestore ناقصة · مفيش Rate Limiting · مفيش إشعارات بريد.

---

## 2. اللي تم إصلاحه في هذه الجلسة ✅

### إزالة الهاي لايت الزجاجي من لوحة التحكم

**المشكلة:** بقعة خضراء شبه شفافة (frosted pill) ظاهرة ورا الكلام في كل بطاقات KPI — أوضحها `رصيد الخزنة الصافي`.

**السبب الجذري:** قاعدتين CSS قديمة في `.kpi-grid` كانت مكتوبة لماركب بطاقات قديم واستمرت تسرّب على البطاقات الحالية:

```css
/* قبل — بتضرب على أي div جوه البطاقة */
.kpi-grid article>div { width:42px; height:42px; border-radius:12px;
                        background:rgba(22,138,112,.12) }
.kpi-grid span       { position:absolute; bottom:23px; left:20px }
```

البطاقات الحالية بتستخدم الـ `div` دا كـ**صف عنوان فيه النص نفسه**، فالقاعدة رسمت بقعة خضراء بـ `border-radius` ورا الكلام. والقاعدة التانية شالت الأيقونة من مكانها ورمتها في رِكن البطاقة.

**الحل:** تحديد نطاق القاعدتين بكلاسات مخصصة بدل عناصر HTML عامة:

```css
/* بعد */
.kpi-grid article>.kpi-icon-box { … }
.kpi-grid article>.kpi-delta    { … }
```

**ملاحظة مهمة:** كوميت سابق (`fe978b4`) كان حاول يصلح نفس المشكلة، لكنه غيّر اسم النسخة الخاصة بالدارك مود بس وفات القاعدة الأساسية — فالمشكلة فضلت. المرة دي القاعدتين الأساسيتين اتصلّحوا.

**نطاق التأثير:** الإصلاح غطّى **كل** شبكات KPI الثلاث: `DashOverview` و `DashTreasury` و `DashCases`.

**التحقق:** فحص بـ headless Chromium في الوضعين الفاتح والداكن → **صفر** عناصر باقية بخلفية غير شفافة أو `position:absolute`. كمان عملنا scan على باقي ملف الـ CSS (2440 سطر) للبحث عن نفس النمط (قواعد بخلفية على عناصر HTML عامة جوه حاويات لوحة التحكم) — الباقي كله محدد النطاق وسليم.

---

## 3. الفحص التفصيلي — اللي ناقص

### 🔴 المرحلة الأولى: أمان وسلامة بيانات (عاجل — قبل أي إضافات)

هذه البنود ليست تحسينات، بل **إصلاحات لمخاطر قائمة**.

#### 3.1 ثغرة: تزوير هوية المتطوّع (خطورة عالية)

في `src/api/volunteers.ts` سطر ~111، طلب التطوع بيقرأ هوية المستخدم من الكوكي **بدون التحقق من التوقيع**:

```ts
const payload = JSON.parse(atob(sessionCookie.split('.')[1]))
profile_id = payload.uid || payload.sub   // ← ثقة عمياء في بيانات المستخدم
```

`atob` بتفكّ تشفير Base64 بس — مش بتتحقّق من إمضاء الـ JWT. أي حد يقدر يعمل كوكي مزيّف بأي `uid` ويربط طلب تطوع بحساب شخص تاني.

**الحل:** استخدام `getAuth(c).verifySessionCookie()` — الدالة موجودة ومستخدمة فعلًا في `middleware.ts`، المطلوب استدعاؤها هنا.

**الجهد:** ساعة واحدة · **الأثر:** يسدّ ثغرة انتحال هوية.

#### 3.2 قواعد Firestore ناقصة لمجموعة كاملة

`firestore.rules` بيغطّي 13 مجموعة، لكن الكود بيستخدم 16. **`beneficiary_groups`** (أرشيف المستفيدين — أكثر البيانات حساسية في الموقع كله: أسماء ناس محتاجة) **مالهاش أي قاعدة**. وكذلك `gallery` و `chunks`.

في Firestore، المجموعة اللي مالهاش قاعدة → مرفوضة افتراضيًا لو الوصول من العميل، لكن الاعتماد على السلوك الافتراضي بدل قاعدة صريحة هو خطر تشغيلي: أول ما حد يضيف قاعدة عامة أو `match /{document=**}` تنكشف البيانات.

**الحل:** إضافة قواعد صريحة `allow read, write: if isAdmin()` لـ `beneficiary_groups`، و `allow read: if true; allow write: if isAdmin()` لـ `gallery`، و `if false` لـ `chunks`.

**الجهد:** ساعة · **الأثر:** حماية بيانات المستفيدين.

#### 3.3 مفيش Rate Limiting فعليًا (خطورة عالية)

دالة `rateLimiter()` **مكتوبة بالكامل في `middleware.ts` لكن مش مستخدمة في أي مكان** (تأكدنا بـ grep على كل المشروع → صفر استخدام). نفس الحكاية مع `validateBody()`.

يعني كل النماذج العامة مفتوحة للإساءة بلا حدود:
- `POST /api/donations/add` — إغراق قاعدة البيانات بتبرعات وهمية
- `POST /api/contacts` — سبام
- `POST /api/newsletter` — سبام
- `POST /api/volunteers` — طلبات وهمية
- `POST /api/upload/public` — **الأخطر:** رفع صور 5 ميجا بلا حد على عدد المرات → استهلاك تخزين وفاتورة

**الحل:** تطبيق `rateLimiter` على كل المسارات العامة الكاتبة، وتطبيق `validateBody` بالمخططات المناسبة. الكود جاهز — المطلوب توصيله.

> ملاحظة: الـ rate limiter الحالي بيخزّن في الذاكرة (`Map`)، وعلى Vercel serverless كل instance له ذاكرته. دا يقلّل فعاليته لكن يبقى أفضل من لا شيء. الحل الأمتن: Firestore counter أو Upstash Redis.

**الجهد:** 3–4 ساعات · **الأثر:** يمنع الإساءة والإغراق.

#### 3.4 مفيش حماية Bot / CAPTCHA

مفيش أي `honeypot` أو `Turnstile` أو `reCAPTCHA` على النماذج العامة. لموقع خيري، السبام على نموذج التبرع بيلوّث بيانات مالية.

**الحل المقترح:** Cloudflare Turnstile (مجاني وخفيف وبيحترم الخصوصية) + honeypot field كخط دفاع أول.

**الجهد:** 3 ساعات · **الأثر:** تنظيف البيانات.

#### 3.5 مفيش أي Security Headers

مفيش `Content-Security-Policy` ولا `X-Frame-Options` ولا `Referrer-Policy` ولا `Strict-Transport-Security`.

**الحل:** middleware واحد في `src/index.tsx` يضيف الترويسات. لازم ياخد في الحسبان الـ CDNs المستخدمة (fonts.googleapis, jsdelivr).

**الجهد:** ساعتين · **الأثر:** حماية من XSS و clickjacking.

---

### 🟠 المرحلة الثانية: سلامة البيانات المالية (عاجل)

#### 3.6 باج: التبرع مش بيتربط بالحملة المختارة (خطورة عالية)

**دا أخطر باج وظيفي في الموقع.**

نموذج التبرع في `src/components/Donate.tsx` بيعرض قائمة اختيار الحملة وبيرسل `campaign_id`:

```tsx
<select name="campaign_id"> … الحملات … </select>
```

لكن المعالج `POST /api/donations/add` **بيهمل القيمة تمامًا** ويكتب قيمة ثابتة:

```ts
campaign_id: null,
campaign_title: 'الصندوق العام',   // ← ثابت، دايمًا
```

**النتيجة:** متبرّع يختار "حملة علاج الأطفال" → تبرعه يتسجّل تحت "الصندوق العام". كل التبرعات الواردة من الموقع بتفقد ربطها بالحملة، والحملات في لوحة التحكم مش بتتحدّث. دا يخلّي أرقام "المُحصّل" في كل صفحات الحملات غير صحيحة، وهي أرقام معروضة للجمهور — يعني مشكلة مصداقية مش بس مشكلة تقنية.

**الحل:** قراءة `body.campaign_id`، التحقق من وجود الحملة، جلب عنوانها وتصنيفها، وتحديث `raised` للحملة عند اعتماد التبرع (المنطق دا موجود فعلًا في مسار تاني في نفس الملف — يتم إعادة استخدامه).

**الجهد:** 2–3 ساعات · **الأثر:** يصحّح البيانات المالية والمصداقية العامة.

#### 3.7 مفيش سجل تدقيق (Audit Trail)

الخزنة تتعدّل وتتحذف بلا أي سجل. لمؤسسة خيرية خاضعة لإشراف وزارة التضامن الاجتماعي (رقم تشهير 3115 لسنة 2026 حسب الفوتر) دا **متطلب حوكمة**، مش رفاهية.

**الحل:** مجموعة `audit_log` تسجّل: مين، إيه العملية، على أي مستند، القيمة قبل وبعد، التاريخ، الـ IP. + شاشة عرض في لوحة التحكم.

**الجهد:** 6–8 ساعات · **الأثر:** حوكمة ومساءلة.

#### 3.8 الحذف نهائي بلا رجعة

كل عمليات الحذف `hard delete`. غلطة واحدة = بيانات ضايعة للأبد.

**الحل:** `soft delete` بحقل `deleted_at` + سلة محذوفات + استرجاع.

**الجهد:** 5 ساعات · **الأثر:** أمان تشغيلي.

#### 3.9 مفيش إيصالات تبرع

المتبرع مش بياخد أي إثبات. دا **معيار أساسي** في أي مؤسسة خيرية محترمة.

**الحل:** إيصال PDF برقم تسلسلي + شعار المؤسسة + رقم التشهير، يتبعت بالبريد ويكون متاح للتحميل من `/profile`. البنية التحتية موجودة (`src/api/export.ts` فيه توليد ملفات بالشعار).

**الجهد:** 8 ساعات · **الأثر:** ثقة المتبرعين.

---

### 🟡 المرحلة الثالثة: التشغيل والموثوقية

#### 3.10 مفيش أي نظام إشعارات (فجوة كبيرة)

فحصنا المشروع كله: **مفيش أي مكتبة بريد** (لا nodemailer ولا SendGrid ولا Resend) ولا SMS.

النتيجة العملية:
- متبرع يتبرع → مايوصلوش أي تأكيد
- متطوع يتقدّم → مايوصلوش أي رد
- حد يبعت رسالة تواصل → **الإدارة مش بتعرف** إلا لما تفتح لوحة التحكم وتشوف بنفسها
- مشترك في النشرة → مفيش ترحيب

**دا معناه إن رسائل الناس ممكن تفضل مهملة أيام.**

**الحل:** Resend أو SendGrid + قوالب بريد عربية RTL + إشعار فوري للإدارة على كل رسالة/طلب جديد.

**الجهد:** 10–12 ساعة · **الأثر:** عالي جدًا — دا الفرق بين موقع "صفحة عرض" وموقع مؤسسة شغّالة.

#### 3.11 مفيش اختبارات ولا CI

`test.js` مش اختبار — دا سطر واحد بيعمل `import` للملف المبني (وكمان مكتوب بترميز UTF-16 غلط). مفيش `.github/workflows`.

**الحل:** Vitest + اختبارات للمسارات الحرجة (حساب الخزنة، صلاحيات الأدمن، ربط التبرع بالحملة) + GitHub Actions يشغّل `tsc` والاختبارات على كل PR.

**الجهد:** 12 ساعة · **الأثر:** يمنع تكرار أخطاء زي باج التبرع.

#### 3.12 مفيش مراقبة أخطاء

الأخطاء بتروح لـ `console.error` وبتضيع في لوجات Vercel. مفيش تنبيه لما حاجة تتعطل في الإنتاج.

**الحل:** Sentry (الخطة المجانية كفاية) + تنبيه على الأخطاء الحرجة.

**الجهد:** 3 ساعات · **الأثر:** تعرف بالمشكلة قبل ما المستخدم يبلّغ.

#### 3.13 مفيش نسخ احتياطي

مفيش أي سكربت أو جدولة لنسخ Firestore. فقدان البيانات = فقدان أرشيف المستفيدين وكل السجلات المالية.

**الحل:** تصدير يومي مجدول + اختبار استرجاع دوري.

**الجهد:** 4 ساعات · **الأثر:** حماية وجودية للبيانات.

#### 3.14 استعلامات بلا حدود (مشكلة أداء وفاتورة)

20+ استعلام بيجيب مجموعات كاملة بلا `limit` ولا ترقيم صفحات (تأكدنا: صفر استخدام لـ `startAfter` في المشروع). أمثلة:

```ts
db.collection('treasury_income').orderBy('created_at','desc').get()   // كل السجلات
db.collection('volunteers').get()                                      // كل المتطوعين
db.collection('donations').orderBy('created_at','desc').get()          // كل التبرعات
```

دلوقتي البيانات قليلة فمش باين. عند 10,000 تبرع دا يبقى: صفحة بتحمّل في 10+ ثواني + فاتورة Firestore reads بتكبر خطيًا.

**الحل:** ترقيم صفحات على الخادم بـ `limit` + `startAfter`، وحساب المجاميع عبر عدّادات مجمّعة بدل جلب كل المستندات.

**الجهد:** 10 ساعات · **الأثر:** أداء وتكلفة على المدى الطويل.

---

### 🟢 المرحلة الرابعة: تحسينات SEO والوصول

#### 3.15 وسوم SEO ناقصة

فحص `shared.tsx`: **صفر** وسوم من: `canonical`، `og:url`، `og:type`، `og:locale`، وكل مجموعة `twitter:*`.

كمان `og:image` بيشاور على `/static/foundation-logo.png` (شعار شفاف) بدل `og-image.png` الموجودة فعلًا في المجلد ومخصصة للمشاركة. النتيجة: مشاركة أي صفحة على فيسبوك أو واتساب تطلع بمعاينة ضعيفة — وهي أهم قناة انتشار لمؤسسة خيرية مصرية.

**الحل:** استكمال الوسوم + استخدام `og-image.png` + `og:locale=ar_EG`.

**الجهد:** 3 ساعات · **الأثر:** انتشار أفضل على السوشيال.

#### 3.16 خريطة الموقع ثابتة وناقصة

`sitemap.xml` فيه 14 مسار ثابت بس، ومابيشملش صفحات المحتوى الديناميكي (الحملات، الأخبار، الفعاليات، القصص) — يعني المحتوى الحقيقي اللي بيجيب زوار من جوجل مش مفهرس. وكمان مفيش `lastmod` ولا `priority`.

**الحل:** توليد ديناميكي من Firestore + `lastmod`.

**الجهد:** 3 ساعات · **الأثر:** فهرسة أفضل.

#### 3.17 بيانات منظّمة (Schema.org) سطحية

`JSON-LD` فيه بيانات المنظمة الأساسية بس. ناقص: `Article` للأخبار، `Event` للفعاليات، `DonateAction`، `BreadcrumbList`، `FAQPage` — ودي كلها بتطلع نتائج غنية في جوجل.

**الجهد:** 5 ساعات · **الأثر:** ظهور أفضل في البحث.

#### 3.18 مراجعة الوصول (Accessibility)

الأساسيات موجودة (`aria-label`, `sr-only`, `prefers-reduced-motion`, 30 من 31 صورة عندها `alt`). محتاج تدقيق كامل: تباين الألوان في الوضعين، التنقل بالكيبورد في المودالات (focus trap)، إعلانات `aria-live` للنماذج.

**الجهد:** 8 ساعات · **الأثر:** شمول — ومتطلب أخلاقي لمؤسسة خيرية.

---

### 🔵 المرحلة الخامسة: مزايا احترافية جديدة

مرتّبة حسب العائد على الجهد:

| # | الميزة | الوصف | الجهد | الأثر |
|---|---|---|---|---|
| 1 | **بوابة دفع إلكترونية** | حاليًا كل التبرعات تحويل يدوي (إنستاباي/فودافون/بنك) والإدارة بتعتمدها يدويًا. دمج Paymob أو Fawry يخلّي التبرع فوري ومؤكد ويرفع معدل الإتمام بشكل كبير | 20 ساعة | ⭐⭐⭐⭐⭐ |
| 2 | **تقارير مالية دورية** | تقرير PDF شهري/سنوي: إيرادات، مصروفات، توزيع على الحملات. متطلب شفافية لمؤسسة مشهرة | 10 ساعات | ⭐⭐⭐⭐⭐ |
| 3 | **التبرع الشهري المتكرر** | أهم مصدر دخل مستقر لأي مؤسسة خيرية. الحقل `donation_type` موجود في البيانات لكن مفيش منطق تكرار | 12 ساعة | ⭐⭐⭐⭐⭐ |
| 4 | **صفحة أثر تفاعلية** | خرائط ورسوم بيانية توضّح فين وصل الخير وكام مستفيد بالمحافظة | 12 ساعة | ⭐⭐⭐⭐ |
| 5 | **بوابة المتطوع** | لوحة للمتطوع: ساعاته، مهامه، شهاداته، تقييمه | 15 ساعة | ⭐⭐⭐⭐ |
| 6 | **إدارة صلاحيات متقدمة** | حاليًا `admin` أو لا. المؤسسة محتاجة أدوار: محاسب (خزنة بس)، محرر محتوى، مشرف متطوعين | 10 ساعات | ⭐⭐⭐⭐ |
| 7 | **كفالة يتيم/حالة** | ربط متبرع بحالة محددة مع تحديثات دورية | 15 ساعة | ⭐⭐⭐⭐ |
| 8 | **PWA + وضع أوفلاين** | تطبيق قابل للتثبيت — مهم جدًا لجودة الشبكة في مصر | 8 ساعات | ⭐⭐⭐ |
| 9 | **بحث شامل في الموقع** | مفيش بحث للزوار حاليًا | 8 ساعات | ⭐⭐⭐ |
| 10 | **لوحة تحكم بالموبايل** | لوحة التحكم مستخدمة على الموبايل لكن الجداول أفقية وصعبة | 10 ساعات | ⭐⭐⭐ |
| 11 | **مركز إشعارات داخلي** | تنبيهات فورية للإدارة داخل اللوحة | 6 ساعات | ⭐⭐⭐ |
| 12 | **تعدد اللغات (عربي/إنجليزي)** | للمانحين والشركاء الدوليين | 20 ساعة | ⭐⭐ |

---

## 4. خطة التنفيذ الزمنية

### الأسبوع 1 — إصلاحات حرجة (لا تُؤجَّل)
- [x] إزالة الهاي لايت الزجاجي من لوحة التحكم ✅
- [ ] 3.1 إصلاح ثغرة تزوير الجلسة في `volunteers.ts`
- [ ] 3.6 إصلاح باج ربط التبرع بالحملة
- [ ] 3.2 استكمال قواعد Firestore الناقصة
- [ ] 3.3 توصيل `rateLimiter` و `validateBody`
- [ ] 3.5 إضافة Security Headers

### الأسبوع 2 — الثقة والتواصل
- [ ] 3.10 نظام البريد والإشعارات
- [ ] 3.9 إيصالات التبرع PDF
- [ ] 3.4 حماية Bot / Turnstile
- [ ] 3.15 استكمال وسوم SEO

### الأسبوع 3–4 — الحوكمة والموثوقية
- [ ] 3.7 سجل التدقيق
- [ ] 3.8 الحذف الآمن + سلة المحذوفات
- [ ] 3.13 النسخ الاحتياطي
- [ ] 3.12 مراقبة الأخطاء (Sentry)
- [ ] 3.11 الاختبارات + CI

### الشهر 2 — التوسّع
- [ ] بوابة الدفع الإلكتروني
- [ ] التقارير المالية الدورية
- [ ] التبرع الشهري المتكرر
- [ ] 3.14 ترقيم الصفحات والأداء
- [ ] 3.16–3.17 SEO المتقدم

### الشهر 3 — التميّز
- [ ] صفحة الأثر التفاعلية
- [ ] بوابة المتطوع
- [ ] الصلاحيات المتقدمة
- [ ] 3.18 تدقيق الوصول الكامل
- [ ] PWA

---

## 5. مؤشرات النجاح

| المؤشر | الحالي | المستهدف |
|---|---|---|
| ثغرات أمنية معروفة | 5 | 0 |
| تغطية الاختبارات | 0% | 60%+ |
| زمن تحميل الصفحة | غير مقيس | < 2.5s |
| Lighthouse — الوصول | غير مقيس | 95+ |
| معدل إتمام التبرع | غير مقيس | يُقاس ثم يُحسّن |
| زمن الرد على رسالة تواصل | أيام (يدوي) | < 24 ساعة (بإشعار فوري) |
| تغطية قواعد Firestore | 13/16 مجموعة | 16/16 |

---

## 6. ملاحظات معمارية

**نقاط قوة نبني عليها:** فصل واضح بين `api/` و `components/` · JSX على الخادم (خفيف وسريع) · نظام تخزين صور بـ 3 طبقات احتياطية (Cloudinary → Firebase Storage → Firestore) وهو تصميم ذكي · معالجة أخطاء عربية متسقة · تعليقات توضح قرارات سابقة (`shared.tsx` و `volunteers.ts`).

**نقاط تحتاج انتباه:**
- `Dashboard.tsx` وصل **1812 سطر** — يُفضّل تقسيمه لمكوّنات لكل قسم
- الأنماط المكتوبة inline كتيرة جدًا — يُفضّل نقلها لكلاسات CSS. **دا بالتحديد هو نفس الجذر اللي سبب مشكلة الهاي لايت:** خلط بين أنماط inline وقواعد CSS عامة بتضرب على عناصر HTML مجردة (`article>div`) بدل كلاسات صريحة. القاعدة العامة اللي تمنع تكرار المشكلة: **أي قاعدة CSS تلوّن خلفية لازم تستهدف كلاس، مش عنصر HTML**
- الـ CSS مضغوط في سطور طويلة جدًا (سطر واحد فيه 2000+ حرف) — دا اللي خلّى المشكلة صعبة الاكتشاف. يُفضّل فصل ملفات CSS لكل قسم

---

## 7. ملحق — تفاصيل نتائج الفحص

**تم فحصه:** 24 مسار صفحة (كلهم شغّالين مع معالجة 404) · 20 ملف API · 17 قسم لوحة تحكم · 2440 سطر CSS · قواعد Firestore · إعدادات البناء

**النتائج بالأرقام:**
- ✅ كل مسارات الصفحات موجودة ومربوطة، ومعالج 404 مخصص
- ✅ كل مسارات الأدمن الكاتبة عليها `adminMiddleware` (تم التحقق مسارًا بمسار)
- ✅ `treasury` و `cases` و `export` عليهم حماية على مستوى الراوتر (`use('*', adminMiddleware)`) — نمط سليم
- ✅ 30 من 31 صورة عندها `alt`
- ⚠️ 3 مجموعات Firestore بلا قواعد صريحة (منها أرشيف المستفيدين)
- ⚠️ 5 نماذج عامة كاتبة بلا rate limiting
- ⚠️ صفر استخدام لـ `rateLimiter` و `validateBody` رغم أنهم مكتوبين بالكامل
- ⚠️ صفر استخدام لـ `startAfter` (مفيش ترقيم صفحات)
- ⚠️ صفر مكتبة بريد أو SMS
- ⚠️ صفر وسوم `twitter:*` أو `canonical`
- ⚠️ صفر اختبارات حقيقية، صفر CI
